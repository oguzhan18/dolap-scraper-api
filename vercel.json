{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "dist/vercel.js",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/vercel.js" }
  ]
}
